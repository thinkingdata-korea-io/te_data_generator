'use client';

/**
 * ScanLines Component
 * @brief: Creates a CRT terminal-style scan line effect overlay
 */
export function ScanLines() {
  return (
    <div className="fixed inset-0 pointer-events-none z-50 scan-lines" />
  );
}
