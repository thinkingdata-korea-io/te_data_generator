# 이벤트 트래킹 데이터 생성기 - 아키텍처 문서

## 개요

AI 기반으로 Excel 텍소노미 스키마를 생성하거나 기존 Excel 파일을 업로드하여 현실적인 서비스 이벤트 데이터를 생성하고 LogBus2로 ThinkingEngine에 전송하는 통합 시스템입니다.

**핵심 특징**:
- **두 가지 시작 모드**: Excel 자동 생성 또는 기존 파일 업로드
- **AI 3단계 텍소노미 생성**: 서비스 정보만으로 4-sheet Excel 스키마 자동 생성
  - Stage 1: 이벤트/속성 설계
  - Stage 2: 데이터 구조 최적화
  - Stage 3: 4-sheet Excel 생성 (#유저 ID 체계, #이벤트 데이터, #공통 속성, #유저 데이터)
- **Multi-Provider AI 지원**: Anthropic Claude / OpenAI GPT 선택 가능
  - Excel 생성과 데이터 생성에 각각 다른 Provider 사용 가능
  - 설정 UI에서 간편하게 변경 가능
- **유저 생명주기 기반**: 현실적인 이벤트 생성 (Faker.js 통합, 국가별 로케일 지원)
- **일자별 JSONL 분리**: 날짜별 파일로 분리 저장
- **LogBus2 통합**: Gzip 압축 전송으로 최적화
- **Excel 다운로드**: 생성된 스키마 파일 다운로드 가능
- **실시간 진행 상태**: 데이터 생성 및 전송 진행률 실시간 모니터링
- **유연한 네비게이션**: 각 단계에서 홈으로 돌아가기 가능

## 시스템 아키텍처

### 전체 흐름도

```
┌─────────────────────────────────────────────────────────────────┐
│                    프론트엔드 (Next.js 14)                       │
│                      http://localhost:3000                       │
│                                                                  │
│  [홈 화면] - 시작 모드 선택                                     │
│                                                                  │
│     ┌──────────────────┐         ┌──────────────────┐          │
│     │   🆕 새로 만들기 │         │  📁 기존 엑셀    │          │
│     │                  │         │     사용하기      │          │
│     │ 1. 정보 입력     │         │                  │          │
│     │ 2. Excel 생성    │         │ 1. 파일 업로드   │          │
│     │ 3. 데이터 생성   │         │ 2. 설정 입력     │          │
│     │ 4. 전송          │         │ 3. 데이터 생성   │          │
│     │                  │         │ 4. 전송          │          │
│     └──────────────────┘         └──────────────────┘          │
│              │                            │                      │
│              └────────────┬───────────────┘                      │
│                           │                                      │
│            [각 단계에서 🏠 홈으로 돌아가기 가능]                 │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    API 서버 (Express)                            │
│                     http://localhost:3001                        │
│                                                                  │
│  API 엔드포인트:                                                 │
│  • GET  /api/settings                  설정 조회 (AI Provider) │
│  • POST /api/settings                  설정 저장                │
│  • POST /api/excel/generate            AI Excel 스키마 생성    │
│  • GET  /api/excel/list                Excel 목록               │
│  • GET  /api/excel/download/:filename  Excel 다운로드          │
│  • POST /api/excel/upload              Excel 업로드 및 파싱    │
│  • POST /api/generate/start            데이터 생성 시작         │
│  • GET  /api/generate/status/:runId    진행 상태 폴링          │
│  • POST /api/send-data/:runId          ThinkingEngine 전송     │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    데이터 생성 엔진                              │
│                                                                  │
│  1. Excel 파싱 → 4-sheet 스키마 추출                            │
│     (#유저 ID 체계, #이벤트 데이터, #공통 속성, #유저 데이터)   │
│  2. AI 분석 (Anthropic/OpenAI) → 이벤트 의존성, 데이터 범위    │
│  3. 유저 코호트 생성 → Faker.js 국가별 현실 데이터              │
│  4. 일자별 이벤트 생성 → 의존성 검증 → JSONL 파일 저장         │
│  5. 진행 상태 콜백 (onProgress) → API 서버 → 프론트엔드        │
│                                                                  │
│  출력: output/data/run_XXX/*.jsonl (날짜별 분리)                │
│        output/runs/run_XXX/metadata.json (실행 정보)            │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    LogBus2 통합 레이어                           │
│                                                                  │
│  1. daemon.json 자동 생성 (gzip 압축 설정)                      │
│  2. LogBus2 프로세스 시작                                        │
│  3. 날짜순 파일 전송 (2025-01-01.jsonl → 01-02 → ...)          │
│  4. 진행 상태 모니터링 (업로드 파일 수/전체 파일)               │
│  5. 전송 완료 후 LogBus2 종료                                   │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│               ThinkingEngine Receiver                            │
│         https://te-receiver-naver.thinkingdata.kr/               │
│                                                                  │
│  • Gzip 압축 데이터 수신                                         │
│  • 이벤트 저장 및 분석                                           │
└─────────────────────────────────────────────────────────────────┘
```

### 모드별 상세 흐름

#### 모드 1: 새로 만들기 (Excel 자동 생성)

```
[홈] → [정보 입력]
         ↓
      시나리오, 산업, 서비스 특징 입력
         ↓
    [Excel 생성 시작] ← 🏠 홈
         ↓
    AI 3단계 프로세스 (TaxonomyBuilderV2)
      Stage 1: 이벤트/속성 설계 (Claude/GPT)
      Stage 2: 데이터 구조 최적화
      Stage 3: 4-sheet Excel 생성
    (excel-schema-generator/output/generated-schemas/)
         ↓
    [Excel 생성 완료] ← 🏠 홈
         ↓
    Excel 미리보기 표시:
      - 이벤트 수, 이벤트 속성 수
      - 공통 속성 수, 유저 데이터 수
         ↓
    [📥 Excel 다운로드 (옵션)]
         ↓
    DAU, 날짜 범위 입력
         ↓
    [데이터 생성 시작]
         ↓
    [데이터 생성 중]
      - 진행률 실시간 표시 (0-100%)
      - 단계별 메시지 (Excel 파싱 → AI 분석 → 유저 생성 → 이벤트 생성)
         ↓
    [데이터 생성 완료] ← 🏠 홈
      - 총 이벤트, 총 사용자, Run ID 표시
         ↓
    APP_ID 입력
         ↓
    [ThinkingEngine 전송]
      - LogBus2를 통한 gzip 압축 전송
      - 진행률 모니터링
         ↓
    [전송 완료] → 🏠 새로운 생성 시작
```

#### 모드 2: 기존 엑셀 사용하기

```
[홈] → [Excel 업로드]
         ↓
      파일 선택 (드래그 앤 드롭 또는 파일 선택)
         ↓
    서버로 업로드 + 자동 파싱
         ↓
    [업로드 완료] ← 🏠 홈
         ↓
    Excel 미리보기 표시:
      - 이벤트 수, 이벤트 속성 수
      - 공통 속성 수, 유저 데이터 수
         ↓
    [서비스 정보 및 설정 입력] ← 🏠 홈
         ↓
      시나리오, DAU, 산업, 서비스 특징, 날짜 범위 입력
         ↓
    [데이터 생성 시작]
         ↓
    [데이터 생성 중]
      - 진행률 실시간 표시 (0-100%)
      - 단계별 메시지
         ↓
    [데이터 생성 완료] ← 🏠 홈
      - 총 이벤트, 총 사용자, Run ID 표시
         ↓
    APP_ID 입력
         ↓
    [ThinkingEngine 전송]
      - LogBus2를 통한 gzip 압축 전송
      - 진행률 모니터링
         ↓
    [전송 완료] → 🏠 새로운 생성 시작
```

## 핵심 라이브러리

### Faker.js 활용
- **@faker-js/faker** - 현실적인 더미 데이터 생성
- 국가별 로케일 지원 (ja, ko, en, zh_CN, zh_TW 등)
- 유저명, 주소, 전화번호, 상품명 등 자동 생성
- 국가별 IP 대역, 통신사, 디바이스 모델 등

## 핵심 컴포넌트

### 1. Excel 텍소노미 파서 (ExcelParser)

**목적**: AI가 생성하거나 사용자가 업로드한 4-sheet Excel 텍소노미 파싱

**입력**:
- excel-schema-generator/output/generated-schemas/ 폴더의 AI 생성 Excel 파일
- 또는 파일 업로드 UI를 통한 사용자 제공 Excel 파일

**출력**:
- ParsedSchema 객체 (이벤트, 속성, 퍼널, 유저 데이터)

**Excel 4-Sheet 구조 (TaxonomyBuilderV2 생성):**
```
📋 Sheet 1: #유저 ID 체계
컬럼: 유형 | 속성 이름 | 속성 별칭 | 속성 설명 | 값 설명
- 계정 ID, 디바이스 ID 등 유저 식별 체계 정의
- ThinkingEngine의 #account_id, #distinct_id 매핑

📋 Sheet 2: #이벤트 데이터
컬럼: 이벤트명 | 이벤트 별칭 | 이벤트 설명 | 속성 이름 | 속성 별칭 | 속성 설명 | 속성 타입 | 예시 값
- 이벤트와 해당 이벤트 전용 속성을 함께 정의
- event_name과 이벤트 전용 property를 한 시트에 표현
- 예: app_start 이벤트 → app_version, platform 등의 속성

📋 Sheet 3: #공통 속성
컬럼: 속성 이름 | 속성 별칭 | 속성 설명 | 속성 타입 | 예시 값
- 모든 이벤트에 공통으로 적용되는 속성
- 예: user_id, session_id, device_model, os_version 등
- event_name이 없는 순수 공통 속성

📋 Sheet 4: #유저 데이터
컬럼: 속성 이름 | 속성 별칭 | 속성 설명 | 업데이트 방식 | 예시 값 | 태그
- user_set, user_setonce, user_add 등의 유저 속성
- 예: user_level (user_set), total_purchase_amount (user_add)
- 태그: 기본 정보, 행동 데이터, 마케팅 등
```

**출력 (TypeScript 타입 정의):**
```typescript
// data-generator/src/types/schema.ts

export interface EventDefinition {
  event_name: string;              // 이벤트 식별자
  event_name_kr: string;           // 한국어명
  category: string;                // system, transaction, interaction 등
  required_previous_events?: string[];  // 선행 이벤트 (AI가 추론)
  user_lifecycle_stage?: string[]; // new, active, returning, churned
  trigger_probability?: number;    // 발생 확률 (0-1)
}

export interface PropertyDefinition {
  property_name: string;           // 속성 키
  property_name_kr: string;        // 한국어명
  data_type: string;               // string, number, boolean, date
  event_name?: string;             // 특정 이벤트에만 속하는 경우 (Sheet 2)
  description?: string;
}

export interface FunnelDefinition {
  name: string;                    // 퍼널 식별자
  description?: string;
  steps: string[];                 // event_name 배열
  conversion_rate?: number;        // 전체 퍼널 전환율
}

export interface UserDataDefinition {
  property_name: string;           // 유저 속성 키
  property_name_kr: string;
  data_type: string;
  update_method: string;           // user_set, user_setonce, user_add
  description?: string;
  tag?: string;                    // 기본 정보, 행동 데이터 등
}

export interface UserSegment {
  name: string;                    // 세그먼트명 (AI가 생성)
  ratio?: number;                  // AI가 결정한 비율
  characteristics?: string;        // AI가 분석한 특성
}

export interface ParsedSchema {
  events: EventDefinition[];       // Sheet 2에서 파싱
  properties: PropertyDefinition[]; // Sheet 2 (이벤트 속성) + Sheet 3 (공통 속성)
  funnels: FunnelDefinition[];     // AI가 추론
  userData: UserDataDefinition[];  // Sheet 4에서 파싱
  userSegments?: UserSegment[];    // AI가 자동 생성 (Excel에는 없음)
}
```

### 2. 유저 코호트 생성기 (Faker.js 통합)

**목적**: 날짜별 신규 유저 생성 및 기존 유저 생명주기 관리

**입력**:
- 시뮬레이션 날짜 범위 (예: 2025-01-01 ~ 2025-01-30)
- DAU (Daily Active Users)
- **AI가 자동 생성한 유저 세그먼트 비율** (Excel에 없음, AI가 시나리오 기반 생성)
  - 예: { new: 10%, core: 15%, regular: 50%, light: 25% }
- **국가별 유저 분포** (기본값: JP 30%, KR 25%, US 20%, CN 15%, TW 10%)

**코호트 생성 로직**:
```typescript
function generateUserCohorts(
  dateRange: DateRange,
  dailyActiveUsers: number,
  newUserRatio: number
): Map<string, User[]> {
  const cohorts = new Map();
  const userPool: User[] = []; // 전체 유저 풀

  for (const date of dateRange) {
    const newUserCount = Math.floor(dailyActiveUsers * newUserRatio);
    const returningUserCount = dailyActiveUsers - newUserCount;

    // 1. 신규 유저 생성 (설치일 = 오늘)
    const newUsers = createNewUsers(newUserCount, date);
    userPool.push(...newUsers);

    // 2. 기존 유저 중 활성화될 유저 선택
    const activeUsers = selectActiveUsers(
      userPool.filter(u => u.installDate < date),
      returningUserCount,
      date
    );

    // 3. 해당 날짜의 전체 활성 유저
    cohorts.set(date, [...newUsers, ...activeUsers]);
  }

  return cohorts;
}
```

**유저 생명주기 상태 (Faker.js 통합)**:
```typescript
interface User {
  id: string;
  installDate: Date;           // 설치일
  lastActiveDate: Date;         // 마지막 활성 날짜
  lifecycleStage: 'new' | 'active' | 'returning' | 'dormant' | 'churned';
  segment: string;              // AI 결정 세그먼트

  // Faker.js 생성 - 국가별 현실적 데이터
  country: string;              // "일본", "대한민국" 등
  countryCode: string;          // "JP", "KR" 등
  locale: string;               // "ja", "ko" (Faker.js 로케일)
  name: string;                 // Faker.js person.fullName() with locale (일본이면 일본어 이름)
  email: string;                // Faker.js internet.email()
  phone: string;                // Faker.js phone.number() with locale
  deviceModel: string;          // Faker.js phone.model()

  sessionHistory: Session[];    // 세션 이력
  completedEvents: Set<string>; // 완료한 이벤트 목록 (의존성 체크용)
  currentLevel: number;
  totalUsageTime: number;
}
```

### 3. 일자별 이벤트 생성기 (Faker.js + AI 결합)

**목적**: 각 날짜별로 현실적인 이벤트 시퀀스 생성

**데이터 생성 우선순위**:
1. **AI 정의 범위 우선** - AI가 분석한 데이터 범위 사용
2. **Faker.js 폴백** - AI 범위 없을 시 속성 이름 기반 Faker.js 생성
3. **국가별 로케일** - 유저 국가에 맞는 Faker 로케일 사용

**프로세스**:

```typescript
async function generateDailyEvents(
  date: Date,
  activeUsers: User[],
  schema: ParsedSchema,
  aiRanges: DataRanges
): Promise<TEEvent[]> {
  const dailyEvents: TEEvent[] = [];

  for (const user of activeUsers) {
    // 1. 유저 생명주기 단계 확인
    const stage = determineLifecycleStage(user, date);

    // 2. 세션 스케줄 생성 (해당 날짜 내)
    const sessions = generateSessionsForDay(user, date);

    for (const session of sessions) {
      // 3. 첫 이벤트는 항상 app_start
      const events = [createEvent('app_start', user, session.start)];

      // 4. 신규 유저면 온보딩 퍼널
      if (stage === 'new' && !user.completedEvents.has('tutorial_complete')) {
        const onboardingEvents = generateFunnelEvents(
          user,
          schema.funnels.find(f => f.name === 'onboarding'),
          session.start
        );
        events.push(...onboardingEvents);
      }

      // 5. 일반 서비스 이벤트 (의존성 준수)
      const serviceEvents = generateServiceEvents(
        user,
        session,
        schema,
        aiRanges
      );
      events.push(...serviceEvents);

      // 6. 세션 종료 이벤트
      events.push(createEvent('app_end', user, session.end));

      // 7. TE 형식으로 변환
      const teEvents = events.map(e => formatToTE(e, user, schema));

      // 8. user_set/user_add 이벤트 생성
      const userUpdates = generateUserUpdates(user, events);

      dailyEvents.push(...teEvents, ...userUpdates);

      // 9. 유저 상태 업데이트
      updateUserState(user, events);
    }
  }

  // 시간순 정렬
  return dailyEvents.sort((a, b) =>
    new Date(a['#event_time']).getTime() - new Date(b['#event_time']).getTime()
  );
}
```

### 4. 이벤트 의존성 관리자

**목적**: 이벤트가 현실적인 순서로 발생하도록 보장

**의존성 그래프**:
```typescript
class EventDependencyManager {
  private graph: Map<string, string[]>; // event -> required previous events

  canEventOccur(
    eventName: string,
    user: User
  ): boolean {
    const dependencies = this.graph.get(eventName) || [];

    // 모든 선행 이벤트가 발생했는지 확인
    return dependencies.every(dep =>
      user.completedEvents.has(dep)
    );
  }

  getNextPossibleEvents(user: User): string[] {
    return Array.from(this.graph.keys()).filter(event =>
      this.canEventOccur(event, user)
    );
  }
}
```

**예시 의존성**:
```
app_install (설치)
  ↓
app_start (앱 시작)
  ↓
sign_up (회원가입)
  ↓
login (로그인)
  ↓
tutorial_start (튜토리얼 시작)
  ↓
tutorial_complete (튜토리얼 완료)
  ↓
stage_1_start (스테이지 1 시작)
  ↓
stage_1_clear (스테이지 1 클리어)
  ↓
iap_purchase (인앱 결제) - 선택적
```

### 5. 일자별 파일 생성기

**목적**: LogBus2가 순차 처리할 수 있도록 날짜별 JSONL 파일 생성

**출력 구조**:
```
output/
├── runs/                          # 실행 메타데이터 (LogBus2와 분리)
│   ├── run_20250105_143022/
│   │   ├── metadata.json          # 실행 설정 정보
│   │   ├── summary.json           # 통계 요약
│   │   └── schema.xlsx            # 사용된 Excel 스키마 백업
│   ├── run_20250105_150033/
│   │   └── ...
│   └── run_20250106_090000/
│       └── ...
└── data/                          # LogBus2가 읽는 순수 데이터
    ├── run_20250105_143022/
    │   ├── 2025-01-01.jsonl       # 1월 1일 이벤트만
    │   ├── 2025-01-02.jsonl       # 1월 2일 이벤트만
    │   ├── 2025-01-03.jsonl
    │   ├── ...
    │   └── 2025-01-30.jsonl
    ├── run_20250105_150033/
    │   └── ...
    └── run_20250106_090000/
        └── ...
```

**폴더 역할**:
- `output/runs/` : 실행 정보, 통계, Excel 백업 (프론트엔드 분석용)
- `output/data/` : 순수 JSONL 데이터 (LogBus2 전송용)

**metadata.json**:
```json
{
  "runId": "run_20250105_143022",
  "createdAt": "2025-01-05T14:30:22Z",
  "config": {
    "schemaFile": "example_service_schema.xlsx",
    "dateRange": {
      "start": "2025-01-01",
      "end": "2025-01-30"
    },
    "dau": 1000,
    "newUserRatio": 0.05,
    "totalUsers": 1500
  },
  "appId": "your_app_id",
  "receiverUrl": "https://te-receiver-naver.thinkingdata.kr/"
}
```

**summary.json**:
```json
{
  "totalEvents": 150420,
  "eventsByDate": {
    "2025-01-01": 5020,
    "2025-01-02": 5100,
    ...
  },
  "eventsByType": {
    "app_start": 30000,
    "stage_clear": 25000,
    ...
  },
  "usersBySegment": {
    "whale": 75,
    "regular": 450,
    "casual": 975
  }
}
```

### 6. LogBus2 통합

**설정 생성**:
```typescript
function generateLogBusConfig(runId: string, appId: string, pushUrl: string) {
  // 순수 데이터 폴더만 지정 (메타데이터 제외)
  const dataPath = path.join(process.cwd(), 'output', 'data', runId);

  return {
    "datasource": [{
      "type": "file",
      "file_patterns": [`${dataPath}/*.jsonl`],  // 오직 JSONL만
      "app_id": appId,
      "http_compress": "gzip",
      "unit_remove": "day",
      "offset_remove": 7
    }],
    "push_url": pushUrl,
    "cpu_limit": 4
  };
}
```

### 7. 파일 보관 관리 시스템

**목적**: 디스크 공간 관리 및 오래된 파일 자동 정리

**구성**:
```typescript
interface FileRetentionConfig {
  dataRetentionDays: number;      // 데이터 파일 보관 기간 (기본: 7일)
  excelRetentionDays: number;     // Excel 파일 보관 기간 (기본: 30일)
  autoDeleteAfterSend: boolean;   // 전송 후 즉시 삭제 (기본: false)
}

class FileRetentionManager {
  // 예약된 정리 작업 (24시간마다 실행)
  scheduleCleanup(): void {
    setInterval(() => {
      this.cleanupOldFiles();
    }, 24 * 60 * 60 * 1000);
  }

  // 보관 기간 초과 파일 삭제
  cleanupOldFiles(): void {
    const now = Date.now();

    // 1. 데이터 파일 정리
    this.cleanupDirectory(
      'output/data/',
      this.config.dataRetentionDays,
      now
    );

    // 2. Excel 파일 정리
    this.cleanupDirectory(
      'excel-schema-generator/output/',
      this.config.excelRetentionDays,
      now
    );

    // 3. 메타데이터 정리
    this.cleanupDirectory(
      'output/metadata/',
      this.config.dataRetentionDays,
      now
    );
  }

  // 전송 완료 후 즉시 삭제
  deleteAfterTransmission(runId: string): void {
    if (this.config.autoDeleteAfterSend) {
      const dataDir = path.join('output/data', runId);
      fs.rmSync(dataDir, { recursive: true, force: true });
      console.log(`🗑️  Auto-deleted: ${dataDir}`);
    }
  }
}
```

**정리 정책**:
- **데이터 파일** (output/data/run_XXX/): 기본 7일 보관
- **Excel 파일** (excel-schema-generator/output/): 기본 30일 보관
- **메타데이터** (output/metadata/run_XXX/): 기본 7일 보관
- **전송 후 즉시 삭제**: 설정 시 전송 완료 즉시 데이터 파일만 삭제

**실행 시점**:
1. 서버 시작 시 초기 정리 1회 실행
2. 24시간마다 예약 실행
3. ThinkingEngine 전송 완료 시 (옵션)

**중요**:
- LogBus2는 `output/data/run_XXX/*.jsonl` 만 읽음
- `output/runs/run_XXX/` 의 메타데이터는 읽지 않음
- 이렇게 하면 LogBus2가 JSON 파일을 이벤트로 오인하는 문제 방지

**순차 업로드 보장**:
- LogBus2는 파일명 순서대로 처리하므로 `YYYY-MM-DD.jsonl` 형식이 자동으로 날짜순 보장

## 프론트엔드 UI 구조

### 화면 흐름 (React State)

```typescript
type ProcessStep =
  | 'select-mode'          // 홈: 모드 선택
  | 'input'                // [새로 만들기] 정보 입력
  | 'generating-excel'     // [새로 만들기] Excel 생성 중
  | 'excel-completed'      // [새로 만들기] Excel 완료 + 다운로드
  | 'upload-excel'         // [기존 엑셀] 업로드 화면
  | 'upload-completed'     // [기존 엑셀] 업로드 완료
  | 'combined-config'      // [기존 엑셀] 설정 입력
  | 'generating-data'      // [공통] 데이터 생성 중
  | 'data-completed'       // [공통] 데이터 생성 완료
  | 'sending-data'         // [공통] ThinkingEngine 전송 중
  | 'sent';                // [공통] 전송 완료
```

### 주요 화면 구성

#### 1. 홈 화면 (select-mode)

```
┌─────────────────────────────────────────────────────────────┐
│  ThinkingEngine                                [⚙️ 설정]    │
│  AI 기반 이벤트 데이터 생성 플랫폼                          │
│                                                              │
│  데이터 생성 방법을 선택하세요                              │
│                                                              │
│  ┌──────────────────────┐    ┌──────────────────────┐     │
│  │ 🆕 새로 시작하기     │    │ 📁 기존 엑셀 사용   │     │
│  │                      │    │                      │     │
│  │ 산업/서비스 정보 →   │    │ 이미 만들어진       │     │
│  │ Excel 자동 생성 →    │    │ 엑셀 업로드 →       │     │
│  │ 데이터 생성          │    │ 데이터 생성         │     │
│  │                      │    │                      │     │
│  │ 1. 서비스 정보 입력  │    │ 1. 엑셀 파일 업로드 │     │
│  │ 2. 엑셀 자동 생성    │    │ 2. 설정 입력        │     │
│  │ 3. 데이터 생성       │    │ 3. 데이터 생성      │     │
│  └──────────────────────┘    └──────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

#### 2. Excel 생성 완료 화면 (excel-completed)

```
┌─────────────────────────────────────────────────────────────┐
│  ✅ Excel 스키마 생성 완료                                  │
│                                                              │
│  Excel 스키마가 성공적으로 생성되었습니다!                  │
│  이제 데이터 생성 설정을 입력해주세요.                      │
│                                                              │
│  데이터 생성 설정                                            │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐           │
│  │ DAU        │  │ 시작 날짜  │  │ 종료 날짜  │           │
│  │ 5000       │  │ 2025-01-01 │  │ 2025-01-30 │           │
│  └────────────┘  └────────────┘  └────────────┘           │
│                                                              │
│  ┌──────────────────────┐  ┌──────────────────────┐       │
│  │ 📥 Excel 다운로드    │  │ 🏠 홈으로            │       │
│  └──────────────────────┘  └──────────────────────┘       │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │         🤖 데이터 생성 시작                      │      │
│  └──────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

#### 3. 데이터 생성 완료 화면 (data-completed)

```
┌─────────────────────────────────────────────────────────────┐
│  ✅ 데이터 생성 완료                                        │
│                                                              │
│  🎉 데이터 생성 완료!                                       │
│                                                              │
│  ┌───────────────┐  ┌───────────────┐                      │
│  │ 총 이벤트     │  │ 총 사용자     │                      │
│  │ 150,420       │  │ 1,532         │                      │
│  └───────────────┘  └───────────────┘                      │
│                                                              │
│  ┌───────────────┐  ┌───────────────┐                      │
│  │ 총 일수       │  │ Run ID        │                      │
│  │ 30일          │  │ run_XXX...    │                      │
│  └───────────────┘  └───────────────┘                      │
│                                                              │
│  ThinkingEngine으로 데이터를 전송하세요.                    │
│                                                              │
│  ┌──────────────────────┐  ┌──────────────────────┐       │
│  │ 🏠 홈으로            │  │ 📤 데이터 전송       │       │
│  └──────────────────────┘  └──────────────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

#### 4. 설정 모달

```
┌─────────────────────────────────────────────────────────────┐
│  ⚙️ 설정                                          [✕ 닫기]  │
│                                                              │
│  Anthropic API Key:                                          │
│  [sk-ant-...]                                               │
│                                                              │
│  TE App ID:                                                  │
│  [1edbbf43c73d4b0ba513f0383714ba5d]                        │
│                                                              │
│  TE Receiver URL:                                            │
│  [https://te-receiver-naver.thinkingdata.kr/]              │
│                                                              │
│  ── 파일 보관 설정 ──────────────────────────────          │
│                                                              │
│  데이터 파일 보관 기간 (일):                                │
│  [7]  생성된 데이터 파일을 자동으로 삭제할 기간            │
│                                                              │
│  Excel 파일 보관 기간 (일):                                 │
│  [30] 생성된 Excel 스키마 파일을 자동으로 삭제할 기간      │
│                                                              │
│  [ ] 전송 후 즉시 삭제                                      │
│      ThinkingEngine으로 전송 완료 후 데이터 파일을          │
│      즉시 삭제                                              │
│                                                              │
│  ┌──────────┐  ┌────────────────────┐                      │
│  │ 취소     │  │ 저장               │                      │
│  └──────────┘  └────────────────────┘                      │
└─────────────────────────────────────────────────────────────┘
```

## 파일 구조

```
demo_data_gen/
├── docs/
│   ├── ARCHITECTURE_KR.md           # 이 파일
│   └── TECHNICAL_SPEC_KR.md
│
├── excel-schema-generator/          # Excel 텍소노미 생성기
│   ├── src/
│   │   ├── schema-generator.ts      # ExcelSchemaGenerator 메인 클래스
│   │   ├── taxonomy-builder-v2.ts   # TaxonomyBuilderV2 (3-stage AI)
│   │   └── types.ts                 # 타입 정의
│   ├── prompts/                     # AI 프롬프트
│   │   ├── stage1-events.md         # Stage 1: 이벤트/속성 설계
│   │   ├── stage2-optimize.md       # Stage 2: 데이터 구조 최적화
│   │   └── stage3-excel.md          # Stage 3: 4-sheet Excel 생성
│   ├── output/
│   │   └── generated-schemas/       # AI 생성 Excel 저장소
│   │       ├── game_taxonomy.xlsx
│   │       ├── commerce_taxonomy.xlsx
│   │       └── ...
│   ├── package.json
│   └── tsconfig.json
│
├── data-generator/                  # 데이터 생성 백엔드 + API
│   ├── src/
│   │   ├── api/
│   │   │   └── server.ts            # Express API 서버 (포트 3001)
│   │   │                            # API: settings, excel/generate, excel/upload,
│   │   │                            #      generate/start, generate/status, send-data
│   │   ├── excel/
│   │   │   └── parser.ts            # ExcelParser (4-sheet 파싱)
│   │   ├── ai/
│   │   │   └── client.ts            # AI API 호출 (Anthropic/OpenAI)
│   │   ├── generators/
│   │   │   ├── cohort-generator.ts  # 유저 코호트 생성 (Faker.js)
│   │   │   ├── event-generator.ts   # 일자별 이벤트 생성
│   │   │   ├── dependency-manager.ts # 이벤트 의존성 관리
│   │   │   └── faker-utils.ts       # Faker.js 국가별 유틸
│   │   ├── formatters/
│   │   │   └── te-formatter.ts      # ThinkingEngine 포맷 변환
│   │   ├── logbus/
│   │   │   └── controller.ts        # LogBus2Controller (daemon.json, 전송)
│   │   ├── data-generator.ts        # DataGenerator 메인 클래스
│   │   └── types/
│   │       ├── schema.ts            # ParsedSchema, EventDefinition 등
│   │       ├── user.ts              # User, UserSegment 타입
│   │       ├── event.ts             # Event 타입
│   │       └── country.ts           # 국가별 설정
│   ├── output/                      # 생성 데이터 출력
│   │   ├── runs/                    # 실행 메타데이터
│   │   │   └── run_XXX/
│   │   │       ├── metadata.json    # 실행 설정 정보
│   │   │       ├── summary.json     # 통계 요약
│   │   │       └── schema.xlsx      # 사용된 Excel 백업
│   │   └── data/                    # LogBus2 전송용 JSONL
│   │       └── run_XXX/
│   │           ├── 2025-01-01.jsonl
│   │           ├── 2025-01-02.jsonl
│   │           └── ...
│   ├── package.json
│   └── tsconfig.json
│
├── frontend/                        # 프론트엔드 (Next.js 14)
│   ├── src/
│   │   └── app/
│   │       ├── page.tsx             # 메인 UI (단일 페이지)
│   │       │                        # - 모드 선택 (새로 만들기/엑셀 업로드)
│   │       │                        # - Excel 생성/업로드
│   │       │                        # - 데이터 생성
│   │       │                        # - ThinkingEngine 전송
│   │       ├── layout.tsx
│   │       └── globals.css
│   ├── next.config.mjs              # API 프록시 설정 (3000→3001)
│   ├── package.json
│   └── tsconfig.json
│
├── uploads/                         # 업로드된 Excel 임시 저장
│   └── timestamp_filename.xlsx
│
├── logbus 2/                        # LogBus2 바이너리
│   ├── logbus                       # 실행 파일
│   └── conf/
│       └── daemon.json              # 런타임 생성 (controller.ts)
│
├── .env                             # 환경 변수
│   # AI Provider 설정
│   # ANTHROPIC_API_KEY=sk-ant-...
│   # OPENAI_API_KEY=sk-...
│   # EXCEL_AI_PROVIDER=anthropic    # Excel 생성용 AI (anthropic|openai)
│   # DATA_AI_PROVIDER=anthropic     # 데이터 생성용 AI (anthropic|openai)
│   #
│   # ThinkingEngine 설정
│   # TE_APP_ID=...
│   # TE_RECEIVER_URL=https://te-receiver-naver.thinkingdata.kr/
│   #
│   # 파일 보관 설정
│   # DATA_RETENTION_DAYS=7
│   # EXCEL_RETENTION_DAYS=30
│   # AUTO_DELETE_AFTER_SEND=false
│
└── package.json                     # 루트 스크립트
    # npm run api      → 백엔드 API 서버 (3001)
    # npm run frontend → 프론트엔드 서버 (3000)
```

### 주요 디렉토리 역할

#### `/excel-schema-generator/`
- **`src/`**: Excel 텍소노미 생성 로직
  - `schema-generator.ts`: 메인 생성기
  - `taxonomy-builder-v2.ts`: 3-stage AI 프로세스
- **`prompts/`**: AI 프롬프트 템플릿 (Stage 1/2/3)
- **`output/generated-schemas/`**: AI 생성 Excel 저장
  - 다운로드 및 재사용 가능
  - 파일명 패턴: `{industry}_taxonomy.xlsx`

#### `/data-generator/output/`
- **`runs/`**: 실행 메타데이터 (프론트엔드 표시용)
  - `metadata.json`: 실행 설정, AI Provider, 날짜 범위 등
  - `summary.json`: 통계 요약 (총 이벤트, 유저 수 등)
  - `schema.xlsx`: 사용된 Excel 스키마 백업
- **`data/`**: 순수 JSONL 데이터 (LogBus2 전송용)
  - 날짜별 파일: `YYYY-MM-DD.jsonl`
  - LogBus2만 읽음 (metadata 제외)

#### `/uploads/`
- 사용자가 업로드한 Excel 임시 저장소
- Multer로 저장 후 파싱
- 파일명 패턴: `{timestamp}_{originalname}.xlsx`

#### `/logbus 2/`
- **`logbus`**: LogBus2 실행 바이너리
- **`conf/daemon.json`**: 런타임 자동 생성
  - LogBus2Controller가 생성
  - appId, receiverUrl, gzip 설정 포함

## 시스템 통합 및 데이터 흐름

### 전체 데이터 파이프라인

```
┌────────────────────────────────────────────────────────────┐
│  [1단계] 프론트엔드 (localhost:3000)                       │
│  - 사용자 입력 수집                                        │
│  - UI 상태 관리 (React State)                              │
│  - 2초마다 진행 상태 폴링                                  │
└────────────────────────────────────────────────────────────┘
              │
              │ HTTP Request (프록시: 3000 → 3001)
              ▼
┌────────────────────────────────────────────────────────────┐
│  [2단계] API 서버 (localhost:3001)                         │
│  - Express REST API                                        │
│  - 멀티파트 파일 업로드 (Multer)                          │
│  - 진행 상태 저장 (Map<runId, Progress>)                  │
│  - 설정 관리 (.env 파일)                                  │
└────────────────────────────────────────────────────────────┘
              │
              │ Direct function call
              ▼
┌────────────────────────────────────────────────────────────┐
│  [3단계] 데이터 생성 엔진                                  │
│  - Excel 파싱 → 스키마 객체                               │
│  - AI 호출 (Claude) → 이벤트 의존성 + 데이터 범위        │
│  - 유저 코호트 생성 → 세그먼트별 유저 풀                  │
│  - 일자별 이벤트 생성 → JSONL 파일 저장                   │
│  - 진행 상태 콜백 → API 서버 → 프론트엔드                 │
└────────────────────────────────────────────────────────────┘
              │
              │ File system (JSONL 파일)
              ▼
┌────────────────────────────────────────────────────────────┐
│  [4단계] LogBus2 통합                                       │
│  - daemon.json 자동 생성 (gzip 압축)                       │
│  - LogBus2 프로세스 시작 (exec)                           │
│  - 파일 업로드 모니터링 (progress 명령)                   │
│  - 전송 완료 후 LogBus2 종료                              │
└────────────────────────────────────────────────────────────┘
              │
              │ HTTPS POST (gzip compressed)
              ▼
┌────────────────────────────────────────────────────────────┐
│  [5단계] ThinkingEngine                                    │
│  - 이벤트 수신 및 저장                                     │
│  - 데이터 분석 대시보드                                    │
└────────────────────────────────────────────────────────────┘
```

### API 호출 시퀀스

#### 새로 만들기 모드

```
프론트엔드                API 서버                  Excel 생성기/데이터 엔진
    │
    │ GET /api/settings
    │ (초기 설정 로드: AI Provider, API Keys)
    │────────────────────>
    │<────────────────────
    │ {ANTHROPIC_API_KEY, OPENAI_API_KEY, EXCEL_AI_PROVIDER, DATA_AI_PROVIDER...}
    │
    │ [사용자: 시나리오, 산업, 특징 입력]
    │
    │ 📊 Excel 생성 시작
    │ POST /api/excel/generate
    │ {scenario, industry, notes}
    │────────────────────>
    │                     ExcelSchemaGenerator 호출
    │                     │
    │                     │ TaxonomyBuilderV2
    │                     │   Stage 1: 이벤트/속성 설계 (Claude/GPT)
    │                     │   Stage 2: 데이터 구조 최적화
    │                     │   Stage 3: 4-sheet Excel 생성
    │                     │
    │                     │ output/generated-schemas/XXX.xlsx
    │                     │ ExcelParser로 파싱 → 미리보기 생성
    │<────────────────────
    │ {file: {path, name}, preview: {events, properties...}}
    │
    │ [Excel 미리보기 표시]
    │
    │ [Excel 다운로드 (옵션)]
    │ GET /api/excel/download/:filename
    │────────────────────>
    │                     res.download()
    │<────────────────────
    │
    │ [사용자: DAU, 날짜 범위 입력]
    │
    │ 🤖 데이터 생성 시작
    │ POST /api/generate/start
    │ {excelPath, scenario, dau, industry, notes, dateStart, dateEnd, aiProvider}
    │────────────────────>
    │                     runId 생성
    │                     progressMap.set(runId, {status: 'starting'...})
    │
    │                     generateDataAsync(runId, config) 비동기 실행
    │<────────────────────
    │ {runId, statusUrl}
    │
    │ [2초마다 폴링 시작]
    │ GET /api/generate/status/:runId
    │────────────────────>
    │                     progressMap.get(runId)
    │<────────────────────
    │ {status, progress: 45%, message: "유저 코호트 생성 중..."}
    │
    │                     DataGenerator.generate()
    │                     │ onProgress 콜백으로 progressMap 업데이트
    │                     │ - Excel 파싱 (10%)
    │                     │ - AI 분석 (30%)
    │                     │ - 유저 생성 (50%)
    │                     │ - 이벤트 생성 (70-95%)
    │                     │ - JSONL 저장 (100%)
    │
    │ [폴링 계속...]
    │ GET /api/generate/status/:runId
    │────────────────────>
    │<────────────────────
    │ {status: 'completed', progress: 100, result: {totalEvents, totalUsers, runId}}
    │
    │ [생성 완료 화면 표시]
    │ - 총 이벤트, 총 사용자, Run ID
    │
    │ [사용자: APP_ID 입력]
    │
    │ 📤 전송 시작
    │ POST /api/send-data/:runId
    │ {appId}
    │────────────────────>
    │                     sendDataAsync(runId, appId) 비동기 실행
    │                     │
    │                     │ LogBus2Controller
    │                     │   - daemon.json 생성 (appId, gzip 설정)
    │                     │   - logbus start
    │                     │   - monitorProgress (3초 폴링)
    │                     │     progressMap 업데이트
    │                     │   - logbus stop
    │<────────────────────
    │ {success: true, statusUrl}
    │
    │ [폴링으로 전송 진행률 표시]
    │ GET /api/generate/status/:runId
    │────────────────────>
    │<────────────────────
    │ {status: 'sending', progress: 75%, message: "전송 중: 22/30 파일"}
    │
    │ [전송 완료]
    │ GET /api/generate/status/:runId
    │────────────────────>
    │<────────────────────
    │ {status: 'sent', progress: 100, sentInfo: {appId, fileSizeMB, receiverUrl}}
    │
    │ [파일 정리 (자동, 설정에 따라)]
    │                     FileRetentionManager
    │                     - AUTO_DELETE_AFTER_SEND=true → 즉시 삭제
    │                     - 또는 DATA_RETENTION_DAYS 기간 후 삭제
```

#### 기존 엑셀 사용 모드

```
프론트엔드                API 서버
    │
    │ 📁 파일 선택 (드래그 앤 드롭)
    │
    │ POST /api/excel/upload
    │ multipart/form-data
    │────────────────────>
    │                     Multer: uploads/ 디렉토리에 저장
    │                     ExcelParser.parseExcelFile(path)
    │                     │ - 4-sheet 파싱
    │                     │ - ParsedSchema 객체 생성
    │                     │ - 미리보기 데이터 추출
    │<────────────────────
    │ {file: {path, name, size}, preview: {events, eventProperties, commonProperties, userData}}
    │
    │ [업로드 완료 화면]
    │ - 이벤트 수, 이벤트 속성 수, 공통 속성 수, 유저 데이터 수 표시
    │
    │ [사용자: 시나리오, DAU, 산업, 서비스 특징, 날짜 범위 입력]
    │
    │ 🤖 데이터 생성 시작
    │ POST /api/generate/start
    │ {excelPath=uploaded, scenario, dau, industry, notes, dateStart, dateEnd, aiProvider}
    │────────────────────>
    │
    │ (이후 새로 만들기 모드와 동일)
    │ - runId 반환 → 폴링 → 생성 완료 → 전송
```

## 데이터 생성 흐름 상세

### Phase 1: Excel 파일 읽기 (프론트엔드)
```
외부 프로그램이 생성한 Excel 파일
  ↓ (excel-schema-generator/output/ 에서 파일 선택)
  ↓
Excel 파일 읽기 (xlsx 라이브러리)
  ↓
ParsedSchema {
  events: [{name: "app_start", category: "system", ...}, ...],
  properties: [{name: "stage_id", type: "string", ...}, ...],
  userSegments: [{name: "whale", percentage: 5, ...}, ...],
  funnels: [{name: "onboarding", events: [...], ...}, ...],
  dependencies: null  // 아직 AI가 추론하지 않음
}
```

### Phase 2: AI 분석 및 범위 생성 (서버 API)
```
AI 입력 {
  // Excel에서 읽은 정보
  events: ["app_start", "tutorial_start", "purchase_complete", ...],
  properties: {
    "product_id": { type: "string", examples: ["prod_001", "prod_002"] },
    "amount": { type: "number", examples: [1000, 5000, 10000] },
    "user_name": { type: "string", examples: [] },  // Faker.js로 생성
    "address": { type: "string", examples: [] }      // Faker.js로 생성
  },

  // 프론트엔드에서 입력받은 정보
  userInputs: {
    scenario: "신규 유저 온보딩에 집중하고, 첫 결제까지의 퍼널을 최적화하고 싶습니다...",
    dau: 1000,
    industry: "커머스",
    notes: "일본 타겟 이커머스, 패션/뷰티 중심",
    dateRange: { start: "2025-01-01", end: "2025-01-30" }
  }
}

**중요**: AI는 다음을 인식:
- user_name, address 등은 Faker.js가 국가별 로케일로 자동 생성
- AI는 product_id, amount 등 비즈니스 로직 중심 범위만 정의
- 국가 설정(일본)에 따라 Faker.js가 일본어 이름, 일본 주소 생성
  ↓ (Claude/OpenAI API 호출)
  ↓
AI 출력 {
  // 1. 유저 세그먼트 비율 자동 결정
  userSegments: {
    new: { ratio: 0.10, description: "신규 유저" },
    core: { ratio: 0.15, description: "핵심 유저 (고과금, 고참여)" },
    regular: { ratio: 0.50, description: "일반 유저" },
    light: { ratio: 0.25, description: "라이트 유저 (저참여)" }
  },

  // 2. 이벤트 의존성 추론
  dependencies: {
    "app_start": [],
    "tutorial_start": ["app_start"],
    "tutorial_complete": ["tutorial_start"],
    "stage_1_start": ["tutorial_complete"],
    "stage_1_clear": ["stage_1_start"],
    "iap_purchase": ["tutorial_complete"]  // 선택적
  },

  // 3. 세그먼트별 데이터 범위 생성
  dataRanges: {
    "stage_clear": {
      "stage_id": {
        "core": { values: ["s5-1", "s5-2", ... "s10-20"], weights: [...] },
        "regular": { values: ["s2-1", ... "s5-20"], weights: [...] },
        "light": { values: ["s1-1", ... "s3-10"], weights: [...] }
      },
      "rewards_gold": {
        "core": { min: 500, max: 5000, distribution: "normal" },
        "regular": { min: 100, max: 1000, distribution: "normal" },
        "light": { min: 50, max: 500, distribution: "normal" }
      }
    }
  },

  // 4. 현실적인 시간 간격 정의
  timingPatterns: {
    "tutorial_start → tutorial_complete": { min: 120, max: 300, unit: "seconds" },
    "app_start → first_event": { min: 2, max: 10, unit: "seconds" },
    "stage_start → stage_clear": { min: 30, max: 180, unit: "seconds" }
  },

  // 5. 세션 패턴 정의
  sessionPatterns: {
    "new": { avgSessionsPerDay: 3, avgDuration: 30, peakHours: [12, 20, 22] },
    "core": { avgSessionsPerDay: 5, avgDuration: 45, peakHours: [9, 12, 18, 21] },
    "regular": { avgSessionsPerDay: 2, avgDuration: 20, peakHours: [12, 20] },
    "light": { avgSessionsPerDay: 0.5, avgDuration: 10, peakHours: [21] }
  }
}
```

### Phase 3: 유저 코호트 생성 (프론트엔드)
```
AI 결정 세그먼트 비율:
  신규 10%, 핵심 15%, 일반 50%, 라이트 25%

DAU: 1,000명
날짜 범위: 2025-01-01 ~ 2025-01-30

  ↓ (AI 비율에 따라 유저 생성)
  ↓
총 유저 풀 생성 (설치일 분산):
  신규: 153명 (30일간 분산 설치)
  핵심: 230명
  일반: 766명
  라이트: 383명

  ↓ (날짜별 활성 유저 배치)
  ↓
Map<Date, User[]> {
  "2025-01-01": [
    { id: "user_0001", installDate: "2025-01-01", segment: "new", stage: "new", ... },
    { id: "user_0002", installDate: "2024-12-20", segment: "core", stage: "active", ... },
    { id: "user_0003", installDate: "2024-12-15", segment: "regular", stage: "returning", ... },
    ...
    (총 1,000명: DAU 유지, 세그먼트별 활동 패턴 적용)
  ],
  "2025-01-02": [
    { id: "user_0005", installDate: "2025-01-02", segment: "new", stage: "new", ... },
    { id: "user_0001", installDate: "2025-01-01", segment: "new", stage: "active", ... },
    ...
    (총 1,000명)
  ],
  ...
}
```

### Phase 4: 일자별 이벤트 생성 (프론트엔드)
```
FOR EACH 날짜 in 범위:
  FOR EACH 유저 in cohorts[날짜]:
    1. 생명주기 단계 확인 (new/active/returning)
    2. 세션 스케줄 생성 (하루 중 몇 번 접속)

    FOR EACH 세션:
      3. app_start 이벤트
      4. 신규 유저 → 온보딩 퍼널 (의존성 체크)
      5. 서비스 이벤트 생성 (가능한 이벤트만)
      6. app_end 이벤트
      7. TE 형식으로 변환
      8. user_set/user_add 생성

  9. 해당 날짜 이벤트 시간순 정렬
  10. JSONL 파일 저장: output/run_XXX/YYYY-MM-DD.jsonl
```

### Phase 5: 파일 작성 예시
```javascript
// output/data/run_20250105_143022/2025-01-01.jsonl (LogBus2가 읽음)
{"#type":"track","#event_name":"app_start","#account_id":"user_0001","#event_time":"2025-01-01 09:15:23.123",...}
{"#type":"track","#event_name":"sign_up","#account_id":"user_0001","#event_time":"2025-01-01 09:15:45.456",...}
{"#type":"user_set","#account_id":"user_0001","#event_time":"2025-01-01 09:15:45.456","properties":{"user_type":"new"}}
{"#type":"track","#event_name":"tutorial_start","#account_id":"user_0001","#event_time":"2025-01-01 09:16:12.789",...}
...

// output/runs/run_20250105_143022/metadata.json (메타데이터)
{
  "runId": "run_20250105_143022",
  "createdAt": "2025-01-05T14:30:22Z",
  "config": {...},
  "aiGenerated": {
    "dependencies": {...},
    "dataRanges": {...}
  }
}

// output/runs/run_20250105_143022/summary.json (통계)
{
  "totalEvents": 150420,
  "eventsByDate": {...},
  "funnelConversions": {...}
}

// output/runs/run_20250105_143022/schema.xlsx (사용된 스키마 백업)
```

## 핵심 알고리즘

### 1. 이벤트 의존성 검증
```typescript
class DependencyManager {
  canFireEvent(eventName: string, user: User): boolean {
    const deps = this.dependencies.get(eventName) || [];

    // 모든 필수 선행 이벤트 완료 확인
    for (const requiredEvent of deps) {
      if (!user.completedEvents.has(requiredEvent)) {
        return false;
      }
    }

    // 동시 발생 불가 이벤트 확인
    const conflicts = this.conflicts.get(eventName) || [];
    for (const conflict of conflicts) {
      if (user.completedEvents.has(conflict)) {
        return false;
      }
    }

    return true;
  }
}
```

### 2. 현실적인 세션 생성
```typescript
function generateSessionsForDay(
  user: User,
  date: Date,
  segment: UserSegment
): Session[] {
  const sessions: Session[] = [];
  const sessionCount = poissonRandom(segment.avg_sessions_per_day);

  for (let i = 0; i < sessionCount; i++) {
    // 유저 타입별 선호 시간대
    const preferredHours = {
      whale: [9, 12, 18, 21],      // 아침, 점심, 저녁, 밤
      regular: [12, 20],            // 점심, 저녁
      casual: [21]                  // 밤에만
    }[user.segment];

    const hour = weightedRandom(preferredHours);
    const minute = randomInt(0, 59);

    const start = new Date(date);
    start.setHours(hour, minute, randomInt(0, 59));

    const duration = normalRandom(
      segment.avg_session_duration_min,
      segment.avg_session_duration_min * 0.3 // 표준편차 30%
    );

    sessions.push({
      start,
      duration,
      end: new Date(start.getTime() + duration * 60 * 1000)
    });
  }

  return sessions.sort((a, b) => a.start.getTime() - b.start.getTime());
}
```

### 3. 유저 생명주기 결정
```typescript
function determineLifecycleStage(user: User, currentDate: Date): LifecycleStage {
  const daysSinceInstall = daysBetween(user.installDate, currentDate);
  const daysSinceLastActive = daysBetween(user.lastActiveDate, currentDate);

  if (daysSinceInstall === 0) {
    return 'new';  // 설치 당일
  }

  if (daysSinceLastActive === 1) {
    return 'active';  // 매일 접속
  }

  if (daysSinceLastActive >= 2 && daysSinceLastActive <= 7) {
    return 'returning';  // 며칠만에 복귀
  }

  if (daysSinceLastActive >= 8 && daysSinceLastActive <= 30) {
    return 'dormant';  // 휴면
  }

  return 'churned';  // 이탈
}
```

## 성능 고려사항

### 최적화 목표
- **1,000 DAU x 30일**: 10초 이내 생성
- **10,000 DAU x 30일**: 60초 이내 생성
- **메모리**: 100,000 이벤트당 100MB 이하

### 최적화 전략
1. **날짜별 청크 처리**: 한 번에 모든 날짜 생성하지 않고 하루씩
2. **Web Worker 사용**: 병렬로 여러 날짜 동시 생성
3. **스트리밍 파일 작성**: 메모리에 모두 저장하지 않고 즉시 파일 작성
4. **AI 범위 캐싱**: 동일 스키마에 대해 재사용

## 검증 체크리스트

생성된 데이터가 현실적인지 확인:

### ✅ 이벤트 순서
- [ ] app_install → app_start → sign_up → login 순서 보장
- [ ] tutorial_start 전에 sign_up 완료
- [ ] stage_N_clear 전에 stage_N_start 발생
- [ ] 모든 세션이 app_start로 시작, app_end로 종료

### ✅ 유저 생명주기
- [ ] 신규 유저는 첫날 온보딩 퍼널 진행
- [ ] 신규 유저 비율이 설정값과 일치 (±5%)
- [ ] D1, D7, D30 리텐션이 정의된 범위 내

### ✅ 시간 분포
- [ ] 이벤트가 날짜별로 균등 분산
- [ ] 세션이 현실적인 시간대에 발생
- [ ] 세션 간 간격이 자연스러움

### ✅ 데이터 품질
- [ ] 모든 필수 시스템 필드 존재
- [ ] 이벤트 시간이 시뮬레이션 범위 내
- [ ] 속성 값이 정의된 데이터 타입과 일치
- [ ] JSONL 파일 형식 유효

## 참고 자료

- **ThinkingEngine 문서**: `docs/ThinkingEngine.md`
- **LogBus2 가이드**: `docs/logbus2.md`
- **프리셋 속성**: `docs/Preset Properties.md`
